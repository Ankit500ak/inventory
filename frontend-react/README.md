# React Frontend for Inventory Allocation System

## Setup

```bash
npm install
npm start
```

The app will run on `http://localhost:3000` and connects to the backend API at `http://localhost:3000/api`.

## Features

- Display available products with current stock levels
- Simple form to place orders
- Real-time order history
- Error handling and user feedback
